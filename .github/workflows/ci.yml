name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  casebrain-validation:
    uses: GlacierEQ/public-actions-runner-host/.github/workflows/casebrain-validation.yml@f9c342c9ecdb1dddbefd64d776ec326b0c0703e8
    with:
      expected_head_sha: ${{ github.event.pull_request.head.sha || github.sha }}
